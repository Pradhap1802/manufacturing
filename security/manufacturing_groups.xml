<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="group_manufacturing_multi_uom" model="res.groups">
        <field name="name">Manage Multi UOM</field>
        <field name="comment">The user will have access to the secondary UoM field on products.</field>
    </record>

    <record id="group_manufacturing_shop_floor" model="res.groups">
        <field name="name">Shop Floor User</field>
        <field name="comment">Access to the custom shop floor execution interface.</field>
        <field name="implied_ids" eval="[(4, ref('mrp.group_mrp_user'))]"/>
    </record>
</odoo>
